export enum TransactionOperationEnum {
  submitting,
  submitted,
  transacting,
  confirmed,
  failed,
  cancelled,
  wrap,
  unwrap,
  batchDelegate,
  removeDelegations,
  delegate,
  selectDataProvider,
  rewardsClaim
}