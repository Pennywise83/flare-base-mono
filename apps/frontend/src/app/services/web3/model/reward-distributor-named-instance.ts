export class RewardDistributorNamedInstance {
    instance: string;
    description: string;
    constructor(instance: string, description: string) {
        this.instance = instance;
        this.description = description;
    }
}